import 'package:flutter/material.dart';

class WholeSellerListView extends StatelessWidget {
  const WholeSellerListView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Wholeseller List');
  }
}
